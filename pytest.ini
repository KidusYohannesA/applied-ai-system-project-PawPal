[pytest]
markers =
    integration: tests that hit the real Anthropic API (require ANTHROPIC_API_KEY)
    eval: LLM-as-judge quality eval (slow, ~$0.05 per run, requires ANTHROPIC_API_KEY)
addopts = -m "not integration and not eval"
